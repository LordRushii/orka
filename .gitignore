node_modules
dist
.output
.wxt
*.log
*.tsbuildinfo
bun.lockb.bak
.DS_Store

# Provider credentials and gateway tokens live here and must never be committed.
.env
.env.*
!.env.example

# Local Freebuff agent state (project id, session metadata).
.freebuff/
